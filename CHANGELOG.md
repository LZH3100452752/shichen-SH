# 更新日志

从 v1.0 到当前版本的完整更新记录。

---

## v1.0 — 初版

首个可用版本，搭建自动救砖的核心框架。

- 启动计数机制：每次开机计数 +1，进桌面稳定后清零
- 五档渐进救援：L1 禁最近 1 个 → L2 禁最近 3 个 → L3 禁全部非白名单 →
  L4 含白名单一起禁 → L5 重命名模块目录彻底移除挂载
- 白名单保护：加入白名单的模块在 L1–L3 中不会被禁用
- 守护自身永久保护
- 快照：救援前保存、随时回滚、黄金配置自动记录
- 多种触发条件：启动计数、开机超时、卡启动阶段、system_server 崩溃、
  Zygote 重启、手动强制标记
- 熔断保护：到达最大等级仍失败则停止自动重启，避免无限重启耗光电量
- WebUI 界面：总览、救援、模块、冲突、快照、日志、设置、工具
- 无 WebUI 的环境可通过模块「执行」按钮跳转浏览器打开
- 作者署名「时尘」，版本 v1.0

---

## v1.1 — 脚本检测与系统防护

- **新增脚本安全检测引擎**
  - 危险代码识别，分 critical / high / medium / low 四级
  - 覆盖整机格式化删除、递归删系统分区、清空模块目录、`dd` 写块设备、
    `mkfs` 格式化、向分区写零、Fork 炸弹、循环重启、杀 Zygote 等
  - 基础解密：base64、gzip、xz、bzip2、ROT13、字符倒序
  - 三种入口：按模块 / 按路径 / 粘贴代码
- **新增重要目录守护**
  - 建立基线 → 定期巡检 → 目录消失或条目骤降记录告警
  - 扫描正在执行危险删除命令的进程
  - 支持只读加固与防护配置备份
- **UI 流畅度优化**：分页面按需渲染、内容未变不写 DOM、搜索防抖、
  rAF 批量写入，移除全屏 filter 与 conic 旋转这两个高开销重绘动画
- 修复部分环境下 WebUI 打不开的问题（CGI 路径解析改用三重兜底：
  固定路径 → 逐级回溯 → 扫描 module.prop 定位）

---

## v1.2 — 解密引擎增强

针对"稍微加强一点加密就解不出来"的问题重写解密引擎。

- Caesar 从只试 ROT13 改为**全 25 种位移暴力**，靠"解码后像不像脚本"自动选最优
- 新增 hex 还原（含 `\x41` 形式与连续 hex 串）
- 新增 base64 url-safe 变体（自动还原 `-_` 并补 padding）、lzma 解压
- 解码轮次 3 → 6
- **分片重组**：先展开变量拼接出完整串，再从中提取载荷
- **新增 Shell 规范化**：还原变量拼接、引号/反斜杠切割、`${IFS}` 绕过三类混淆
- 新增"解出来既没命中危险规则、又不比原文更像脚本则判定未解出"的闸门，
  避免返回误导性的假解密链
- 复杂样本耗时约 1.5 秒，普通脚本 0.15 秒，正常模块脚本不误报

---

## v1.3 — 加载反馈与动效

- **全屏分步加载遮罩**：渐变进度环（带百分比）+ 进度条 + 步骤清单，
  步骤逐条点亮、完成后打勾
- 顶部不确定进度条
- 骨架屏：首屏与各页面加载中显示占位块
- 扫描线动效：检测脚本时来回扫的光带
- 按钮内联加载态、结果卡片依次淡入、页面切换位移过渡
- 修复切到「冲突 / 防护 / 日志」页时自动加载回调写成裸标识符导致的
  ReferenceError，此前数据永远停在"加载中"
- 修复 `guard()` 未返回 Promise 导致首屏错误提示卡片显示不出来
- 日志自动刷新改为静默模式

---

## v1.4 — 按钮失效修复

- **修复带参数按钮全部静默失效**：`guard()` 包装函数未接收及转发参数，
  导致快照删除、模块启用/禁用、加白名单、模块体检的 id 全是 `undefined`
- 修复加载圆环 SVG `transform-origin` 默认为坐标原点导致的动画异常
  （拆成外层 div 转圈 + circle 只做进度）
- 加载遮罩增加最小显示时长，解决 KernelSU 同步 exec 下遮罩一闪而过
- 顶栏标题改为「时尘自动救砖守护」

---

## v1.5 — 刷新与卡顿

- 修复 KernelSU 下加载遮罩不显示（`ksu.exec` 同步阻塞，遮罩未上屏就执行完）
- **修复操作后不自动刷新**：内部刷新调用被 `guard()` 的忙碌锁拦截，
  影响快照删除、模块启停、加白名单、救援、设置保存、回滚等 12 处
- 设备信息缓存（20 秒），避免每次刷新跑二十多个子进程
- 首屏渲染优化缓解 KSU 卡顿
- 全局版本号统一（此前执行按钮显示 v1.1、UI 显示 v1.3、module.prop 是 v1.4）

---

## v1.6 — 进度条修复与页面精简

- **修复 KernelSU 下进度条卡在 0%**：进度原由 JS 定时器推进，而 KSU 的 exec
  阻塞主线程，定时器停摆。改为 CSS 合成层动画驱动，主线程阻塞时仍推进
- 页面由 10 个精简为 7 个：快照 → 救援子页，冲突 → 模块子页，日志 → 工具子页
- **新增救援演练**：按真实救援逻辑推演各等级影响范围，全程不改任何东西
- 新增诊断包导出、一键清理、日志只看告警、模块文件查看
- 安装动画改为实时进度条
- 修复旧安装脚本中 `$MODDIR` 未定义导致版本号永远读不到的问题

---

## v1.7 — LOGO 与模块保护

- **修复 KernelSU 下百分比长时间停在 0% 后突然跳到 90%**：
  自定义属性动画走主线程，改为 `translateY` 数字滚轮，
  与进度条、步骤共用同一条时间轴
- 进度条改为匀速并延长时长
- 加入品牌 LOGO（支持 `assets/img/logo.png` 替换）与金属蓝银配色、启动页
- **新增模块锁定（排除名单）**：强于白名单，任何救援等级都不会禁用
- 新增模块备份与还原、清空救援历史
- 新增界面动效 / 紧凑模式 / 金属配色开关
- 安装脚本加入实时进度条

---

## v1.8 — 进阶救砖

- 修复启动页 LOGO 与进度条重叠（`img` 与 `svg` 改为绝对定位叠放）
- 新增问题反馈入口（邮箱 3100452752@qq.com，一键复制 + 导出诊断包）
- **新增按卡死阶段精准救援**：卡在 `post-fs-data` 只禁带该阶段脚本的模块，
  卡在 `service` 只禁带 `service.sh` 的，其他模块完全不动
- 新增修复模块文件权限、修复 SELinux 标签、清理损坏模块目录
- 新增系统健康检查：关键系统文件缺失、存储空间告急、损坏模块、
  SELinux 状态、覆盖 `/system` 的模块数量

---

## v1.9 — 计数与筛选

- **修复启动计数永不自动清零**：清零逻辑只写在 watchdog 里，而它在部分环境
  会被清理。新增 `boot-completed` 阶段延时确认兜底；同时修复 watchdog
  单实例检测靠 `/proc/PID/cmdline` 判断（进程名实际是 `sh`）导致的重复拉起
- 修复模块页筛选条高亮不跟随（只重画列表未同步按钮状态）
- 刷新提速：模块体积 `du` 结果缓存 120 秒
- 修正隔离残留文件清理的判定笔误

---

## v2.0 — 救援等级精简

- **救援等级由 5 级精简为 3 级**：删除原 L1（禁最近 1 个）与 L2（禁最近 3 个），
  原 L3 / L4 / L5 依次成为新的 L1 / L2 / L3
  - L1：禁用全部非白名单模块
  - L2：连同白名单一起禁用，仅留守护自身
  - L3：重命名模块目录，彻底移除挂载
- 最大救援等级上限同步由 5 收紧为 3，设置项改为「最大救援等级(1-3)」
- 修复救援等级超过上限时状态残留未钳制数值（导致进度显示 L9/L3 对不上）
- 界面等级卡片、演练输出、危险按钮阈值同步更新

---

## v2.1 — 云更新

- **新增云更新**：打开界面自动静默检测，有新版才弹窗
- 手动检测更新、更新源连通性测试
- **支持强制更新版本**：云端 `force` 字段为 1 时弹窗无取消按钮、点遮罩关不掉
- 下载后优先调用面具官方安装命令（`ksud` / `apd` / `magisk --install-module`），
  失败自动降级为解压覆盖
- 安装前自动保存快照，装完提示重启，有问题可回滚
- 设置可切换稳定 / 测试通道与自定义源
- LOGO 内置（原 2.19MB 压缩至 81KB）

---

## v2.2 — 云更新默认化

- 修复进入界面弹出 `data is not defined` 错误提示
  （首屏加载回调里误引用了上一段作用域的变量名）
- 修复防护页守护目录点「移除」无效（只删了清单未同步删基线，而界面读的是基线）
- **云更新改为默认行为**：打开界面自动静默检测、仅稳定版通道，移除相关设置项
- 工具页不再展示更新源链接，只显示「更新源 N」与状态

---

## v2.3 — 即时刷新与速度

- 修复防护页守护目录添加/移除后不立即刷新（内部刷新被忙碌锁拦截）
- **设置改为改动即自动保存**，移除手动保存按钮
- 全面缩短加载等待：遮罩单步 0.62s → 0.34s、下限 1.8s → 0.75s、
  收尾停顿 260ms → 120ms、首屏让步减半
- **更新分为「下载」与「安装」两段独立进度**，下载阶段推进到 70% 停住
- 安装完成后自动清理下载包与临时文件

---

## v2.4 — 更新检测准确性

- 修复有更新时点「检测更新」报错 `Cannot read properties of null`
  （`guard()` 包装时 this 传成了 null，而该方法内用 `var self = this` 调 `showUp`）
- **修复云端已改版本但检测更新仍认旧版本**：原逻辑"第一个源成功就停止"，
  若该源是 jsDelivr 的 CDN 缓存旧内容就一直拿不到新版，而"测试更新源"
  会继续尝试后续源，于是两者结果不一致。改为遍历全部源取 `versionCode` 最大者，
  并给每个源加时间戳参数规避缓存

---

## v2.5 — 卡死修复（重要）

- **【严重】修复打开界面即卡死至需强制重启**
  - 根因：打开界面时自动检测云端更新，串行访问多个源，而单个源的 curl
    总超时被设为 `8 × 6 = 48 秒`，最坏近 200 秒
  - KernelSU 的 `exec` 同步阻塞主线程，界面完全无响应；浏览器走异步 CGI
    所以不卡 —— 这正是两者表现不同的原因
  - 修复：单源超时收紧、增加全局时间预算（超预算立即停止）、
    拿到比本地新的版本即停止
- **【严重】修复检测脚本时卡死**
  - 扫描引擎对超大文件逐行正则 + 多层解码，4MB 文件耗时约 50 秒
  - 修复：超过 512KB 只扫前 512KB，且加密检测与解密同样作用于截断后的内容
  - 实测 4MB 文件从 50788ms 降到约 2200ms
- **修复 KernelSU 打开界面慢**：设备信息要跑二十多个子进程，原缓存仅 20 秒，
  重启后必然失效导致每次全量采集。缓存延长到 10 分钟
- 更新源顺序调整，静默检测延后 1.2 秒发起，不与首屏抢主线程

---

## v2.6 — 备用源恢复

- 恢复 Gitee 作为更新源之一（国内直连备用，无 CDN 缓存）
- 当前共 4 个源，自动取版本号最大的那份
- 清理安装脚本中残留的旧版本更新说明与重复输出
- 移除安装脚本中的自定义 LOGO 提示
- 版本号全局统一

---

## v2.7 — 多语言初版

- 新增 10 种语言切换：简体中文、繁體中文、English、日本語、한국어、
  Русский、Français、Español、Deutsch、Tiếng Việt
- 设置页新增「界面语言」下拉，语言同时存入 localStorage 与模块配置
- 以界面中文原文为 key，渲染后遍历文本节点替换，未翻译词条自动回落中文
- 移除 README.md，并在安装与开机脚本中清理模块目录里的残留文件

---

## v2.8 — 多语言重做

- **改为每种语言一个独立 js 语言包**，放在 `assets/js/lang/` 下按需加载，
  中文为内置源文本不加载额外文件，首屏不受影响
- **替换改在 HTML 字符串写入 DOM 之前**，用正则只替换标签之间的文本，
  绝不破坏 `onclick` / `data-*` 属性中的中文参数
  （v2.7 用 `TreeWalker` 遍历 DOM 文本节点，在部分 WebView 上不触发，
   表现为"切了语言又变回中文"）
- **修复切换语言后刷新又变回中文**：切换期间与已应用后不再用接口返回的
  旧配置覆盖当前语言（原来 `setconf` 异步，接口可能仍返回旧值）
- 切换语言立即原地重绘当前页、导航与静态节点，无需退出界面重开
- 词条扩充至约 380 条，覆盖导航、页面标题、卡片、按钮、状态、
  提示语、设置项、设备信息字段等
- 对照界面文本实测：精确覆盖 75%，含部分命中 93%
- 顶栏标题「时尘自动救砖守护」随语言变化

---

## 版本命名说明

- `version`：显示用的版本名，形如 `v2.8`
- `versionCode`：纯数字比较依据，形如 `280`
- 两者必须同步递增，云端 `update.json` 中 `versionCode` 填小或相同都不会弹更新
- 云端 `force` 字段为 `1` 时为强制更新版本，用户不更新无法关闭弹窗
